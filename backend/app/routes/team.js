import createRoutesFromModel from 'business-chat-backend/routes/createRoutesFromModel';
import { teamModel } from 'business-chat-backend/model/Models';

export default createRoutesFromModel(teamModel);
